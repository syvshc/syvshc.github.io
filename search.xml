<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Win10+Ubuntu18.04LTS双系统安装记录</title>
    <url>/2020-12-12-Windows-and-Ubuntu/</url>
    <content><![CDATA[<p>本文记录过程的时候会同时记录踩过的坑, 这些坑有一些是由于我自己的操作不当, 还有一些是由于网上的教程版本过旧或书写错误造成的, 我都会记录下来, 但是文中的一些文本的描述会与系统标准输出的文本有一定差异, 只要不影响命令的运行的文字笔者不会过分追求一字不差. 那么我们开始吧<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a>.</p>
<span id="more"></span>
<h2 id="双系统安装准备">双系统安装准备</h2>
<ul>
<li>在<a href="https://cn.ubuntu.com/">Ubuntu官网</a>下载<code>Ubuntu18.04LTS</code>. 虽然<code>Ubuntu19.10</code>已经问世, 但是考虑到<code>LTS</code>版本的长期支持, 还是选择了这个版本;</li>
<li>关闭<code>Windows10</code>中电源设置中的<code>快速启动</code>;</li>
<li>下载<code>UltraISO</code>作为启动盘制作工具;</li>
<li><code>SanDisk USB3.0 32G</code> U盘一个作为启动盘;</li>
<li>在<code>BIOS</code>中关闭<code>Secure Boot</code>.</li>
<li>在硬盘中<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a>压缩出至少<code>25G</code>的空间作为<code>Ubuntu</code>的系统盘, 我因为考虑到可能长期使用, 压缩了<code>100G</code>, 但是现在已经有不够用的趋势了……</li>
</ul>
<p>在我尝试压缩我的机械硬盘的时候<code>Windows</code>提示我</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-df63444c0adf72d52ae7240ae7bc97f1.png" alt="" /><figcaption>压缩提示</figcaption>
</figure>
<p>而我的机械硬盘上还有<code>300G</code>的空余空间, 我没多想, 只是认为是磁盘里碎片太多, 导致无法找出<code>100G</code>连续的空间进行压缩, 于是我运行了磁盘优化, 但是优化完还是报错, 这时我感觉问题不像我想的那样, 问了<a href="google.com.hk"><code>google</code></a>才知道我的磁盘已经在我不知道是什么的时候变成了<strong>动态磁盘</strong>[^3]! [^3]:在<code>Windows</code>自带的磁盘管理中动态磁盘为黄色, 基本磁盘为深蓝色.</p>
<p>而这块动态磁盘还被分成了4个区, 尽管我的机械硬盘在<code>Win</code>中显示为一块磁盘. 这下知道问题所在就该着手解决了, 查了<a href="google.com.hk">google</a>知道可以用<code>分区助手</code>或<code>DiskGenius</code>来实现动态磁盘逆转回基本磁盘, 在用移动硬盘备份好机械硬盘里所有的文件后开始运行软件, 应该是我的磁盘型号的问题, 这两个软件都<strong>无法处理</strong>, 只能将其格式化, 转换为基本磁盘后再压缩出<code>100G</code>.</p>
<h2 id="开始操作">开始操作</h2>
<h3 id="安装ubuntu18.04">安装<code>Ubuntu18.04</code></h3>
<ul>
<li><p>用<code>UltraISO</code>将<code>ubuntu1804</code>刻录进U盘;</p></li>
<li><p>重启电脑, 进入<code>BIOS</code>, 选择<code>USB</code>启动;</p></li>
<li><p>进入<code>Ubuntu</code>的<code>grub</code>界面后会选择<code>试用(try ubuntu without installing)</code>还是<code>安装(install ubuntu)</code>, 都可以, 如果选择<code>Try ubuntu</code>会在桌面的左上角出现一个<code>install</code>, 双击即可. 选择<code>安装Ubuntu</code>.</p></li>
<li><p>选择语言为<code>汉语</code></p></li>
<li><p>选择<code>为图像或无线硬件, 以及MP3和其它媒体安装第三方软件</code></p></li>
</ul>
<p>我取消了勾选, 而由于我的<code>安装Ubuntu时下载更新</code>默认勾选且无法选择就只能勾选</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-bf16d4403832c000d0d7f3dabebb6332.png" alt="" /><figcaption>准备安装</figcaption>
</figure>
<p>但是在后续的使用中发现缺少了很多依赖, 并不知道是不是安装时这个选项造成的, 但是还是勾选上吧, 只是安装时间会长一些.</p>
<ul>
<li>选择安装类型</li>
</ul>
<figure>
<img src="https://pic4.zhimg.com/80/v2-a98750890a22b38fd625febad744072b.png" alt="" /><figcaption>安装类型</figcaption>
</figure>
<p>我看到的几乎所有的教程中都提到了要选择<code>其他选项</code>, 而正常来说应该在<code>其他选项</code>的下一个界面中出现<code>100G</code>的空闲空间才对, 但是我只有几个<code>1M</code>的空闲空间, 为此我还返回<code>Windows</code>重新格式化了机械硬盘, 重新压缩卷, 但是都没有用. 后来问了我的同学, 他表示直接选择第一个选项<code>安装Ubuntu, 与其他系统共存</code>就可以, 开机时可以自动使用<code>Ubuntu</code>的<code>grub2</code>引导系统, 选完了之后开始安装, 重启电脑.</p>
<p><strong>又出问题了.</strong> 我的系统重启后可以进入<code>Ubuntu</code>的<code>grub2</code>, 选择进入<code>Ubuntu</code>, 在开机图标界面停留了很长时间, 我就强制关机了, 开机再进入<code>Ubuntu</code>的时候同样是停在了开机图标界面, 当我打算再次强制关机甚至重装系统的时候, 电脑自己关机了, <strong>我不确定这和我第一次的强制关机是否有关系</strong>. 在我查了一些文档后发现可能和我的<code>nvidia</code>显卡有关, 即<code>Ubuntu</code>中没有<code>nvidia</code>显卡的驱动, 而独显又主动启动, 导致了开机时的<code>bug</code>, 这里我尝试了两种方法, 均可以进入系统</p>
<ul>
<li>在<code>grub2</code>界面将光标停在<code>Ubuntu</code>, 按<code>e</code>, 找到<code>quiet</code>, 在后面加上<code>nouveau.modeset=0</code>, 按<code>F10</code>保存, 进入系统.</li>
<li>在<code>grub2</code>界面选择第二个选项<code>Advance</code>, 然后选择<code>Resume boot</code>, 进入系统.</li>
</ul>
<p>进入系统后要开始下载<code>nvidia</code>的驱动， 打开终端， 输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ubuntu-drivers devices</span><br></pre></td></tr></table></figure>
<p>可以看到类似下面的输出</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">== /sys/devices/pci0000:00/0000:00:01.0/0000:01:00.0 ==</span><br><span class="line">modalias : pci:v000010DEd00001C8Dsv00001028sd000007E1bc03sc02i00</span><br><span class="line">vendor   : NVIDIA Corporation</span><br><span class="line">model    : GP107M [GeForce GTX 1050 Mobile]</span><br><span class="line">driver   : nvidia-driver-430 - distro non-free</span><br><span class="line">driver   : nvidia-driver-435 - distro non-free recommended</span><br><span class="line">driver   : nvidia-driver-390 - distro non-free</span><br><span class="line">driver   : xserver-xorg-video-nouveau - distro free <span class="built_in">builtin</span></span><br></pre></td></tr></table></figure>
<p>可以看到我的显卡型号是<code>GP107M [GeForce GTX 1050 Mobile]</code>, 推荐的驱动是<code>nvidia-driver-435 - distro non-free recommended</code>, 我使用的是默认安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo ubuntu-drivers autoinstall</span><br></pre></td></tr></table></figure>
<p>也可以使用<code>apt</code>安装, 比如<code>sudo apt install nvidia-340</code>可以安装<code>340</code>版本的驱动. 安装驱动后就可以重启电脑, 这时不会卡开机界面了[^4] [^4]:在我这里没有问题但是不代表没有其他问题.</p>
<h3 id="安装ubuntu下的基本软件">安装Ubuntu下的基本软件</h3>
<h4 id="安装文本编辑器vim">安装文本编辑器<code>vim</code></h4>
<p>在终端输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install vim </span><br></pre></td></tr></table></figure>
<p>即可安装, 可以在终端中输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vimtutor</span><br></pre></td></tr></table></figure>
<p>可以查看<code>vim</code>自带的简易教程, 但是如果终端是<code>UTF8</code>编码, 中文是<code>LANG</code>环境, 那么这个命令是看不到中文版的教程的, 可以在终端运行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iconv -f gb2312 -t utf8 /usr/share/vim/vim80/tutor/tutor.zh.euc -o ~/vimtutor.txt</span><br></pre></td></tr></table></figure>
<p>其中的<code>vim80</code>为自己的<code>vim</code>版本, 如果不确定的话可以<code>cd \usr/share/vim &amp;&amp; ls</code> 看一下, 会将<code>vimtutor.txt</code>的中文版输出到<code>~</code>目录下.</p>
<h4 id="安装texlive">安装<code>TexLive</code></h4>
<p>关于安装教程可以在啸行的<a href="https://github.com/OsbertWang/install-latex">install-LaTeX项目</a>处下载， 有很详细的教程， 这里我就说几个要注意的点.</p>
<ul>
<li><strong>先卸载<code>Ubuntu</code>预装的<code>TeXLive</code></strong>，本来我以为<code>Ubuntu</code>是不自带<code>TeXLive</code>的, 就直接装了<code>TL2019</code>, 但是当我想要用</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr update --self --all</span><br></pre></td></tr></table></figure>
<p>升级宏包时, 提示我<code>TeXLive</code>的版本不匹配</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-94ddf1ee3e06abb707dfe0e226a44dad.png" alt="" /><figcaption>升级宏包错误</figcaption>
</figure>
<p>而且当我使用<code>tex -v</code>查看版本的时候, 发现输出的是<code>TeXLive2017</code>, 然后可以按照<code>install-latex</code>中提到的方法卸载, 虽然安装后再卸载也没出什么问题, 但是安装前还是卸一下好一点.</p>
<ul>
<li>中文索引引擎<code>zhmakeindex</code></li>
</ul>
<p>在<code>Windows</code>添加这个引擎并不难, 从<a href="https://ctan.org/pkg/zhmakeindex">CTAN</a>上下载, 或从leo-liu的<a href="https://github.com/leo-liu/zhmakeindex">zhmakeindex项目</a><code>clone</code>到本地, 但是后者需要自己用<code>go</code>编译, 具体教程可以参考<a href="https://www.ituring.com.cn/article/207412">图灵社区</a>, 有必要一提, 我在照教程实践的时候, 在<strong>安装zhmakeindex</strong>一步中并没有完成, 报了一个我看不懂的错</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-6241ad17c3d2436d93bb804d7021ed93.png" alt="" /><figcaption>go报错</figcaption>
</figure>
<p>由于我不会<code>GOLANG</code>, 也就没有去查相关原因, 知道的大佬可以在评论区补充.</p>
<p>在<code>Windows</code>下直接将编译好的<code>zhmakeindex.exe</code>复制到<code>C:\texlive\2019\bin\win32</code>即可, 在命令行中进入写有索引的文件夹中, 执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zhmakeindex filename</span><br></pre></td></tr></table></figure>
<p>即可生成索引文件 <code>filename.ind</code>.</p>
<p>而在<code>Ubuntu</code>下编译失败后我也尝试用这种方法将<code>zhmakeindex</code>直接复制进<code>/usr/local/texlive/2019/bin/x86_64-linux</code>, 然后执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zhmakeindex filename</span><br></pre></td></tr></table></figure>
<p>但是<code>Ubuntu</code>并不认识这个命令, 在群里交流了一下,</p>
<p>在下载的目录里执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls -hal</span><br></pre></td></tr></table></figure>
<p>会返回下面的结果</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-6241ad17c3d2436d93bb804d7021ed93.png" alt="" /><figcaption>查看 zhmakeindex 权限</figcaption>
</figure>
<p>可以发现<code>zhmakeindex</code>没有执行权限<code>x</code>. 于是执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x zhmakeindex</span><br></pre></td></tr></table></figure>
<p>为<code>zhmakeindex</code>添加执行权限, 在该文件夹下的终端运行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zhmakeindex</span><br></pre></td></tr></table></figure>
<p>如果得到以下的输出</p>
<figure>
<img src="https://pic4.zhimg.com/80/v2-edd3a958503aff3401e4e42103cc6b4a.png" alt="" /><figcaption>zhmakeindex 输出</figcaption>
</figure>
<p>即表示可以执行, 这时可以选择<code>sudo cp ./zhmakeindex /bin/</code>方便调用, 或者在<code>~/.bashrc</code>或<code>~/.zshrc</code>中添加别名后更新</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> zhmakeindex=<span class="string">&quot;/usr/local/texlive/2019/bin/x86_64-linux/zhmakeindex&quot;</span></span><br></pre></td></tr></table></figure>
<p>其中<code>/usr/local/texlive/2019/bin/x86_64-linux/</code>为存有改过权限的<code>zhmakeindex</code>文件的目录</p>
<p>然后</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.zshrc</span><br></pre></td></tr></table></figure>
<p>即可.</p>
<h4 id="安装matlab">安装<code>MATLAB</code></h4>
<p>装<code>MATLAB</code>没有什么好说的, 我的学校提供了教育版的<code>MATLAB</code>, 所以不涉及到破解, 一切按流程走就行, 注意最后填写可使用的用户时的用户名填对.</p>
<h4 id="安装vscode及配置">安装<code>VSCode</code>及配置</h4>
<p>可以从<a href="https://code.visualstudio.com/">官网</a>上下载<code>*.deb</code>文件安装, 或者从<code>Ubuntu软件</code>中安装, 我选择的是后者.</p>
<p>暂时我对<code>VSCode</code>的使用就是作为<code>LaTeX</code>的编辑器, 下载的插件有</p>
<ul>
<li><code>LaTeX Workshop</code></li>
<li><code>Markdown All in One</code></li>
<li><code>Chinese (Simplified) Language Pack for Visual Studio Code</code></li>
</ul>
<p><code>settings.json</code>的内容在<a href="https://paste.ubuntu.com/p/PhQJSbByZd/">这里</a></p>
<p>另外根据我的<code>packages</code>使用习惯增加了用户代码片段<code>latex.json</code>, 在<a href="https://paste.ubuntu.com/p/WS6CBxxY2G/">这里</a></p>
<h4 id="安装通讯工具wineqq">安装通讯工具<code>wine+qq</code></h4>
<p>由于腾讯最近发布的<code>QQ for Linux2.0</code>体验极差, 还是只能选择<code>wine+QQ!</code>的方式解决, 具体教程可以看<a href="https://www.lulinux.com/archives/1319">撸Linux</a>上的教程, 在我的电脑上没有问题, 可以完美的使用.</p>
<h4 id="输入法fcitx-rime以小鹤音形配置">输入法<code>fcitx-rime</code>以小鹤音形配置</h4>
<p>这个东西在我用<code>linux</code>虚拟机的时候就把我折磨地痛不欲生, 网上现存的教程太少了, 只能参考其他教程顺藤摸瓜.</p>
<p>在安装小鹤音形之前, 我曾使用自带的<code>ibus</code>输入法中的双拼简单替代, 使用体验并不是太好, 出现的问题有<strong>输入法响应不及时</strong>, 以及在部分软件中<strong>无法被呼出</strong>, 比如<code>TeXStudio</code>, 放弃. 也使用过<code>Sougou</code>中的双拼, 出现的问题有<strong>候选区乱码</strong> , 并且尝试了解决办法后依然没有解决, 还有一点不符合我使用习惯: <strong>无法设置在中文输入方式下输入半角符号</strong> , 最终还是选择<code>rime</code>挂接码表, 但是还有一个问题就是最新几版的码表已经被隐藏了, 在<a href="http://flypy.ys168.com/">小鹤音形网盘</a>中的码表资源较旧, 但也不是不能用, 就凑合一下吧.</p>
<ol type="1">
<li>首先下载<code>fcitx-rime</code></li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install fcitx-rime</span><br></pre></td></tr></table></figure>
<ol start="2" type="1">
<li>在<a href="http://flypy.ys168.com/">小鹤音形网盘</a>中下载<code>小鹤音形Rime平台for Linux.zip</code>, 解压后会发现有一个<code>bin</code>文件夹, 还有几个<code>*.yaml</code>文件.</li>
<li>按照我找到的几乎所有教程都说要把所有文件复制到<code>~/.config/fcitx/rime</code>下, 但是这么做的话在部署的时候报错, 看一下<code>log</code>文件, 里面有为数不多的我能看懂的</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">E1021 19:55:48.464576 31050 deployment_tasks.cc:242] source file &#39;flypyplus.dict.yaml&#39; does not exist.</span><br><span class="line">E1021 19:55:48.492090 31050 deployment_tasks.cc:242] source file &#39;flypy.dict.yaml&#39; does not exist.</span><br></pre></td></tr></table></figure>
<p>表示<code>rime</code>找不到上面两个文件, 而<code>rime</code>要用这两个文件来生成对应的<code>bin</code>文件来调用, 但是在鹤大提供的包里直接把编译结果<code>bin</code>文件给出来了, 那就是为什么<code>rime</code>找不到这两个文件的问题了. 4. 将<code>bin/</code>文件夹下的所有<code>*.bin</code>文件都拿出来, 放到<code>~/.config/fcitx/rime</code>下, 再进行部署就行了. 5. 也可以在这个文件夹下的<code>flypy_user.txt</code>, <code>flypy_top.txt</code>, <code>flypy_sys.txt</code>来添加自己的编码, 这里就不赘述了.</p>
<h4 id="小飞机">小飞机</h4>
<p>由于某些原因, 这里只贴一个<a href="https://github.com/qingshuisiyuan/electron-ssr-backup">项目链接</a></p>
<h3 id="配置zsh">配置<code>zsh</code></h3>
<p>因为最近刚学了<code>git</code>， 并且有项目在<a href="https://github.com/syvshc">Github</a>上, 而<code>Ubuntu</code>自带的<code>bash</code>并不显示<code>git</code>的分支等信息, 用起来并不是很顺手, 于是用<code>zsh</code>来美化一下. 安装及配置的教程可以看<a href="https://segmentfault.com/a/1190000015283092">这里</a>.</p>
<h2 id="尾声">尾声</h2>
<p>至此我的<code>Ubuntu</code>的配置已经可以满足日常使用, 分配出来的<code>100G</code>空间还剩下<code>25G</code>, 看起来还是不太够用, 不过对于不在<code>Linux</code>下玩游戏来说使用也够了, 就这样吧. 如果有表述错误还请评论区指正</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>由于事先没有准备, 导致很多情况下都没有截图, 这里会尽量找图来保证记录的易读性.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>尽量不要是<code>Windows</code>的系统盘, 并没有试过如果是系统盘会发生什么<a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>配置记录</category>
        <category>帮助文档</category>
      </categories>
      <tags>
        <tag>系统安装</tag>
        <tag>配置记录</tag>
        <tag>帮助问题</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020-12-12-hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="quick-start">Quick Start</h2>
<h3 id="create-a-new-post">Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="run-server">Run server</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="generate-static-files">Generate static files</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="deploy-to-remote-sites">Deploy to remote sites</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>杂项</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>beamer 的字体设置</title>
    <url>/2021-03-03-beamer-font-set/</url>
    <content><![CDATA[<p>在使用 <code>beamer</code> 文档类的时候, 可能会有需要更换字体.</p>
<span id="more"></span>
<p>如果按照 <code>article</code> 的方式设置正文字体:</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;beamer&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;amsmath, fontspec&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;frame&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">\setmainfont</span>&#123;Fira Sans&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line"><span class="keyword">\end</span>&#123;frame&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p>会发现字体并没有发生变化</p>
<p><img src="https://pic4.zhimg.com/80/v2-a79642cc1fef73b11effa52b21dc02ca.png" /></p>
<p>查阅 <code>texdoc beamer</code> 的 18.1 节会发现如果想使用其他字体, 需要设置 <code>\usefonttheme&#123;professionalfonts&#125;</code>, 但是添加了该命令后修改依旧失败.</p>
<p>在群里咨询后得知, <code>\setmainfont&#123;Fira Sans&#125;</code> 命令修改的字体为 <code>rmfamily</code>, 默认为衬线体. 所以要先将字体设置为衬线体 <code>\usefonttheme&#123;serif&#125;</code>, 再进行修改:</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;beamer&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;amsmath, fontspec&#125;</span><br><span class="line"><span class="keyword">\usefonttheme</span>&#123;serif&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;frame&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. </span><br><span class="line">    Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">\setmainfont</span>&#123;Fira Sans&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit. </span><br><span class="line">    Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line"><span class="keyword">\end</span>&#123;frame&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p><img src="https://pic4.zhimg.com/80/v2-2078e554f7891d8ae674b0dd7d87a97b.png" /></p>
<p>另外, 因为 <code>beamer</code> 文档类默认正文使用无衬线体, 于是也可以偷鸡地设置 <code>\setsansfont&#123;Fira Sans&#125;</code> 但是这样做会导致数学模式的 <code>\mathrm</code> 变成衬线体</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;beamer&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;amsmath&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;fontspec&#125;</span><br><span class="line"><span class="keyword">\setsansfont</span>&#123;Fira Sans&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;frame&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit.</span><br><span class="line">    Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line">    <span class="keyword">\[</span></span><br><span class="line">        <span class="keyword">\int</span><span class="built_in">_</span>&#123;0&#125;<span class="built_in">^</span>&#123;<span class="keyword">\pi</span>&#125;<span class="keyword">\sin</span> x<span class="keyword">\,</span><span class="keyword">\mathrm</span>&#123;d&#125;x=2 </span><br><span class="line">    <span class="keyword">\]</span></span><br><span class="line"><span class="keyword">\end</span>&#123;frame&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p><img src="https://pic4.zhimg.com/80/v2-94edf62ee34ec47955526aabbba5a438.png" /></p>
<p>在 <code>texdoc fontspec</code> 中查找原因, 可以在 3.2 节 Maths fonts adjustments 中找到</p>
<p><img src="https://pic4.zhimg.com/80/v2-6ded00e03780caf7534fb7a3e5f7a9a9.png" /></p>
<p>即在默认设置下, <code>fontspec</code> 宏包会将数学字体设置成 <code>Computer Modern</code>, 于是我们需要用 <code>no-math</code> 选项来阻止它, 同时 <code>\mathrm&#123;d&#125;</code> 也会调用 <code>rmfamily</code> 的字体, 我们将它改为 <code>\mathsf&#123;d&#125;</code>:</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;beamer&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>&#123;amsmath&#125;</span><br><span class="line"><span class="keyword">\usepackage</span>[no-math]&#123;fontspec&#125;</span><br><span class="line"><span class="keyword">\setsansfont</span>&#123;Fira Sans&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;frame&#125;</span><br><span class="line">    Lorem ipsum dolor sit amet, consectetuer adipiscing elit.</span><br><span class="line">    Utpurus elit, vestibulum ut, placerat ac, adipiscing vitae, felis.</span><br><span class="line">    <span class="keyword">\[</span></span><br><span class="line">        <span class="keyword">\int</span><span class="built_in">_</span>&#123;0&#125;<span class="built_in">^</span>&#123;<span class="keyword">\pi</span>&#125;<span class="keyword">\sin</span> x<span class="keyword">\,</span><span class="keyword">\mathsf</span>&#123;d&#125;x=2</span><br><span class="line">    <span class="keyword">\]</span></span><br><span class="line"><span class="keyword">\end</span>&#123;frame&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure>
<p>这个暂时不知道是什么原因导致的. 如果想正文和数学公式都使用 Fira 系列的字体, 同时保留衬线字体, 可以如下设置: <figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="keyword">\documentclass</span>&#123;beamer&#125;</span><br><span class="line"><span class="comment">% \usepackage[no-math]&#123;fontspec&#125;</span></span><br><span class="line"><span class="keyword">\usepackage</span>&#123;unicode-math&#125;</span><br><span class="line"><span class="keyword">\usefonttheme</span>&#123;serif&#125;</span><br><span class="line"><span class="comment">% \setsansfont&#123;Fira Sans&#125;</span></span><br><span class="line"><span class="keyword">\setmainfont</span>&#123;Fira Sans&#125;</span><br><span class="line"><span class="keyword">\setsansfont</span>&#123;CMU Serif&#125;</span><br><span class="line"><span class="keyword">\setmathfont</span>&#123;Fira Math&#125;</span><br><span class="line"><span class="keyword">\begin</span>&#123;document&#125;</span><br><span class="line">    <span class="keyword">\begin</span>&#123;frame&#125;</span><br><span class="line">        Handgloves, <span class="built_in">$</span><span class="keyword">\sin</span><span class="built_in">$</span>, <span class="built_in">$</span><span class="keyword">\mathrm</span>&#123;Handgloves&#125;<span class="built_in">$</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">\textsf</span>&#123;this is CMU Serif&#125;</span><br><span class="line">    <span class="keyword">\end</span>&#123;frame&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;document&#125;</span><br></pre></td></tr></table></figure> <img src="https://pic4.zhimg.com/80/v2-84d784c2f5396f6b2e529e0d7506b38d.png" /></p>
<p>如果设置中文字体, 使用 <code>ctexbeamer</code> 文档类并设置 <code>\setCJK(main, sans, mono)font</code>, 类比上文即可.</p>
]]></content>
      <categories>
        <category>LaTeX</category>
        <category>字体</category>
      </categories>
      <tags>
        <tag>字体设置</tag>
        <tag>LaTeX</tag>
        <tag>示例</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建 github.io</title>
    <url>/2021-03-03-build-github-io/</url>
    <content><![CDATA[<p><strong>在 NexT 主题更新 8.0 之后又重新配置了一次 <code>hexo+NexT</code>, 这次来记录一下, 并且会随时更新新增的配置.</strong></p>
<span id="more"></span>
<h2 id="准备工作">准备工作</h2>
<p>假定用户的电脑上已经安装 <code>git</code>, 并且已经与 <a href="github.com">github</a> 连接, 并熟悉 <code>git</code> 的相关操作.</p>
<h3 id="创建仓库">创建仓库</h3>
<p>在 <a href="github.com">github</a> 上新建一个仓库, 名称为 <code>&lt;username&gt;.github.io</code>, 将 <code>&lt;username&gt;</code> 替换为你的 <code>github</code> 账户名. ### 安装 <code>node.js</code> 在 <a href="https://nodejs.org/en/">node.js的官网</a> 上下载 <code>LTS</code> 版本安装, 安装后在命令行输入 <code>npm -v</code>, 如果有版本号输出, 那么说明安装成功, 如果返回 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;npm&#x27;</span> 不是内部或外部命令，也不是可运行的程序或批处理文件。</span><br></pre></td></tr></table></figure> 那么将 <code>&lt;InstallPath&gt;/nodejs</code> 添加到环境变量. 然后命令行运行 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure> 安装 <code>hexo</code>, 确保安装后可以运行 <code>hexo</code> 命令. ### 初始化博客 用 <code>cmd</code> 进入想存放博客的文件夹, 运行 <code>hexo init &lt;blogname&gt;</code>, 将 <code>&lt;blogname&gt;</code> 替换成博客的项目名, 比如 <code>hexotest</code>. 因为 <code>hexo init</code> 的过程要链接 <code>github</code>, 如果出现卡顿, 可以尝试看这个<a href="https://weilining.github.io/201021.html">博客</a>尝试解决.</p>
<p>初始化结束后会看到这样的文件树</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexotest</span><br><span class="line"> ├── db.json</span><br><span class="line"> ├── node_modules</span><br><span class="line"> ├── package-lock.json</span><br><span class="line"> ├── package.json</span><br><span class="line"> ├── public</span><br><span class="line"> ├── scaffolds</span><br><span class="line"> ├── source</span><br><span class="line"> ├── themes</span><br><span class="line"> ├── _config.landscape.yml</span><br><span class="line"> └── _config.yml</span><br></pre></td></tr></table></figure>
<h2 id="部署博客">部署博客</h2>
<h3 id="本地部署">本地部署</h3>
<p>我们进入 <code>hexotest</code> 文件夹, 输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo g</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure>
<p>会有如下输出</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">INFO  Validating config</span><br><span class="line">INFO  Start processing</span><br><span class="line">INFO  Hexo is running at http://localhost:4000 . Press Ctrl+C to stop.</span><br></pre></td></tr></table></figure>
<p>这时在浏览器中输入 <code>localhost:4000</code> 即可看到部署在本地的博客</p>
<p><img src="https://pic4.zhimg.com/80/v2-8c21ffd0058d9cba4be5da45c49f1663.png" /></p>
<p>然后我们命令行输入 <code>ctrl+C</code> 来结束本地部署.</p>
<h3 id="在线部署">在线部署</h3>
<p>当然我们写博客的目的大多都是要上传到互联网, 于是我们要把博客部署到 <code>&lt;username&gt;.github.io</code>.</p>
<p>打开 <code>hexotest</code> 下的 <code>_config.yml</code> 配置文件, 找到 <code>deploy</code>, 填入以下信息</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Deployment</span></span><br><span class="line"><span class="comment">## Docs: https://hexo.io/docs/deployment.html</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">&lt;git</span> <span class="string">repo</span> <span class="string">ssh</span> <span class="string">address&gt;</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>
<p>再运行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo d</span><br></pre></td></tr></table></figure>
<p>等待部署成功, 成功后就可以在 <code>&lt;username&gt;.github.io</code> 上看到博客的内容了.</p>
<h2 id="创建博文">创建博文</h2>
<h3 id="新建博文">新建博文</h3>
<p>在 <code>hexotest/</code> 文件夹下, 用 <code>cmd</code> 执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo n <span class="string">&quot;&lt;blog_name&gt;&quot;</span></span><br></pre></td></tr></table></figure>
<p>将 <code>&lt;blog_name&gt;</code> 替换为博文的名字, 注意不能有空格, 这里我们使用 <code>hexo n first-blog</code>, 成功后会在 <code>source/_posts/</code> 文件夹下看到 <code>first-blog.md</code> 文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">source</span><br><span class="line">└── _posts</span><br><span class="line">   ├── first-blog.md</span><br><span class="line">   └── hello-world.md</span><br></pre></td></tr></table></figure>
<p>同时我们还发现了另一个文件 <code>hello-world.md</code>, 这就是我们在本地部署成功后看到的那一篇博文.</p>
<p>用熟悉的编辑器打开 <code>first-blog.md</code>, 将其内容修改为</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 第一篇博文</span><br><span class="line">date: 2021-03-04 00:32:07</span><br><span class="line">tags:</span><br><span class="line">    - [博文]</span><br><span class="line">    - [测试]</span><br><span class="line">    - [whatever]</span><br><span class="line">categories:</span><br><span class="line">    - [测试, 测试的子分类]</span><br><span class="line">---</span><br><span class="line">这是我的第一篇博文</span><br></pre></td></tr></table></figure>
<p>其中 <code>tags</code> 与 <code>categories</code> 可以自行设置, 它们的关系可以参考这个<a href="https://www.zhihu.com/question/19561626">问题</a></p>
<p>再使用 <code>hexo g &amp;&amp; hexo s</code> 进行本地部署, 在 <code>localhost:4000</code> 看到刚刚编辑的博文"第一篇博文", 点进去可以看到</p>
<p><img src="https://pic4.zhimg.com/80/v2-797a1b83cb08f7653adcb43d56838b8c.png" /></p>
<p>然后可以再使用 <code>hexo d</code> 将其推送到 <code>&lt;username&gt;.github.io</code>, 等待一会如果没有更新内容, 那么就执行一下 <code>hexo clean</code> 来清空部署缓存. 于是我们将博文推送到远端的命令可以归为</p>
<p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d</span><br></pre></td></tr></table></figure></p>
<h2 id="美化界面">美化界面</h2>
<h3 id="配置基本信息">配置基本信息</h3>
<p>配置文件 <code>hexotest/_config.yml</code> 是对应于我们博客的最高级的配置文件, 里面有一些字段可以进行配置, 作为示例修改了下面几项</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Site</span><br><span class="line">title: SYVHSC&#39;s BLOG</span><br><span class="line">subtitle: &#39;&#39;</span><br><span class="line">description: &#39;无锤乙醇的博客&#39;</span><br><span class="line">keywords: LaTeX, 数学</span><br><span class="line">author: 无锤乙醇</span><br><span class="line">language: zh-CN</span><br><span class="line">timezone: Asia&#x2F;Shanghai</span><br></pre></td></tr></table></figure>
<p>进行本地部署可以看到</p>
<p><img src="https://pic4.zhimg.com/80/v2-8121a42938033c596ae6e0a8022d1bcd.png" /></p>
<h3 id="更换主题">更换主题</h3>
<p>这里我们选用 <code>NexT</code> 主题进行配置. 由于 <code>NexT8.0+</code> 已经支持 <code>npm</code> 安装, 于是可以在目录 <code>hexotest/</code> 下用 <code>cmd</code> 执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-theme-next</span><br></pre></td></tr></table></figure>
<p>并在 <code>hexotest/_config.yml</code> 中找到 <code>theme</code> 字段, 修改为 <code>theme: next</code>, 保存后本地部署, 可以看到博客的主题已经更换</p>
<p><img src="https://pic4.zhimg.com/80/v2-d115746d176f5fb3a6aa6ac7d05e73ba.png" /></p>
<p>然后我们可以把 <code>node_modules/hexo-theme-next/</code> 中的 <code>_config.yml</code> 改为 <code>_config.next.yml</code> 并且复制到根目录 <code>hexotest/</code> 下进行修改, 这样当我们升级 <code>NexT</code> 主题的时候不需要重新配置.</p>
<h3 id="下面配置-next-主题的一些事项-这里只介绍一些功能的作用-并不具体配置.">下面配置 <code>NexT</code> 主题的一些事项, 这里只介绍一些功能的作用, 并不具体配置.</h3>
<ol type="1">
<li><strong>scheme</strong> <code>NexT</code> 主题内置了 4 种样式, 可以依次启用来选择一个适合自己的, 这里我选择了 <code>Gemini</code>;</li>
<li><strong>favicon</strong> 配置标签页的图标, 可以自行选择图片, 放在 <code>hexotest/source/img</code> 文件夹下便于寻找, 设置中的路径也应改为 <code>/img/***.png(jpg, svg...)</code>;</li>
<li><strong>menu</strong> 配置博客页面的链接;</li>
<li><strong>social</strong> 配置博客页面的外部链接, 图标可以在 <a href="https://fontawesome.com/">fontawsome</a> 进行选择;</li>
<li><strong>avatar</strong> 配置博客侧边栏的头像, 图片选择同 <strong>favicon</strong>;</li>
<li><strong>toc</strong> 配置浏览博文时侧边栏的目录;</li>
<li><strong>font</strong> 配置文章中的字体; 在 <code>_config.next.yml</code> 中不能配置多种字体, 如果想配置多种字体, 可以修改 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexotest\node_modules\hexo-theme-next\source\css\_variables\base.styl</span><br></pre></td></tr></table></figure> 中的 <code>\\Font families</code> 中的值</li>
<li><strong>codeblock</strong> 配置代码块;</li>
<li><strong>back2top</strong> 配置返回顶部的按钮;</li>
<li><strong>reading_progress</strong> 配置阅读进度条;</li>
<li><strong>symbols_count_time</strong> 配置站点字数和阅读时间统计, <a href="https://github.com/next-theme/hexo-word-counter">使用方法</a>;</li>
<li><strong>creative_commons</strong> 配置文末版权信息, 关于 cc 协议可以参考<a href="https://zhuanlan.zhihu.com/p/20641764">知乎专栏</a>;</li>
<li><strong>local_search</strong> 配置搜索栏, <a href="https://github.com/theme-next/hexo-generator-searchdb">使用方法</a>;</li>
<li><strong>links_settings &amp; links</strong> 配置友情链接;</li>
</ol>
<h3 id="这是一些不知道放到哪里的配置">这是一些不知道放到哪里的配置</h3>
<ol type="1">
<li><p><strong>markdown-it-plus</strong> 可以提供脚注等功能, 目录, emoji, katex 等功能, <a href="https://github.com/CHENXCHEN/hexo-renderer-markdown-it-plus">使用方法</a> emoji 功能与 <code>hexo-renderer-pandoc</code> 冲突;</p></li>
<li><p><strong>hexo-renderer-pandoc</strong> 提供 mathjax 渲染, 将 <code>tags</code> 设置为 <code>ams</code> 还可以使用公式编号<a href="https://theme-next.js.org/docs/third-party-services/math-equations">使用方法</a>;</p>
<p>示例: <figure class="highlight latex"><table><tr><td class="code"><pre><span class="line">一元二次方程组 <span class="built_in">$</span>ax<span class="built_in">^</span>&#123;2&#125;+bx+c=0<span class="keyword">\ </span>(a <span class="keyword">\ne</span> 0)<span class="built_in">$</span> 的解为 <span class="built_in">$</span><span class="keyword">\eqref</span>&#123;eq:slo&#125;<span class="built_in">$</span></span><br><span class="line"><span class="comment">% 注意: 行内公式的 $...$ 前后不要有空格</span></span><br><span class="line"><span class="keyword">\begin</span>&#123;equation&#125;<span class="keyword">\label</span>&#123;eq:slo&#125;</span><br><span class="line">     x<span class="built_in">_</span>&#123;1, 2&#125;=<span class="keyword">\frac</span>&#123;-b<span class="keyword">\pm</span><span class="keyword">\sqrt</span>&#123;b<span class="built_in">^</span>&#123;2&#125;-4ac&#125;&#125;&#123;2a&#125;</span><br><span class="line"><span class="keyword">\end</span>&#123;equation&#125;</span><br></pre></td></tr></table></figure> 一元二次方程组 <span class="math inline">\(ax^{2}+bx+c=0\ (a \ne 0)\)</span> 的解为 <span class="math inline">\(\eqref{eq:slo}\)</span><br />
<span class="math display">\[\begin{equation}\label{eq:slo}
     x_{1, 2}=\frac{-b\pm\sqrt{b^{2}-4ac}}{2a}
\end{equation}\]</span></p></li>
</ol>
<h2 id="未完待续">未完待续</h2>
]]></content>
      <categories>
        <category>配置记录</category>
        <category>帮助文档</category>
      </categories>
      <tags>
        <tag>配置记录</tag>
        <tag>帮助文档</tag>
      </tags>
  </entry>
  <entry>
    <title>tlmgr 的介绍</title>
    <url>/2021-03-04-tlmgr-intro/</url>
    <content><![CDATA[<!-- [点击跳转](#jump) <span id="jump">跳转到的地方</span> -->
<p>这篇文章是用来熟悉 TeX Live 自带的包管理器 <code>tlmgr(TeX Live Manager)</code> 的使用, 官方文档位于 <a href="http://tug.org/texlive/doc/tlmgr.html">TUG</a>. <del>这可能会变成一个翻译文档</del></p>
<span id="more"></span>
<p><strong>注意</strong>:</p>
<ol type="1">
<li>文档中被中括号<code>[]</code>框起来的为可选参数, 如 <code>--gui [action]</code>,</li>
<li>被尖括号<code>&lt;&gt;</code>框起来的为必要参数, 如 <code>tlmgr info &lt;what&gt;</code>,</li>
<li>被 <code>|</code> 分割开的选项为 n 选 1, 如 <code>backup [option] &lt;pkg|-all&gt;</code>,</li>
</ol>
<h2 id="概要">概要</h2>
<p><code>tlmgr</code> 管理着 TeX Live 中的宏包以及配置. 最新的文档以及测试版本的信息位于 <a href="https://tug.org/texlive/tlmgr.html">https://tug.org/texlive/tlmgr.html</a> .</p>
<p>TeX Live 由一些最高级别的 schemes 组成, 每一种 scheme 都是由不同的 collections 与 packages 组成, 其中 collection 是 packages 的集合, 而 packages 是包含了一些文件的包. Schemes 里一般既有 collections 也有 packages, 但是每一个 package 都属于且只属于一个 collection.</p>
<p>可以在 <a href="https://tug.org/texlive/doc">TeX Live 文档</a> 查看 TeX Live 的全部可用文档.</p>
<h2 id="基本格式">基本格式</h2>
<p><code>tlmgr</code> 使用的基本格式为: <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr [option...] &lt;action&gt; [option...] [operand...]</span><br></pre></td></tr></table></figure></p>
<h2 id="例子">例子</h2>
<p>成功安装 TeX Live 后, 可以设置一些 <code>tlmgr</code> 上的常用配置:</p>
<h3 id="tlmgr-option-repository-ctan"><code>tlmgr option repository ctan</code></h3>
<p>告诉 <code>tlmgr</code> 它可以从一个附近的 CTAN 镜像去获取最近的更新, 这条命令还可以写成 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr option repository http://mirror.ctan.org/systems/texlive/tlnet</span><br></pre></td></tr></table></figure> 其中 <code>ctan</code> 是 <code>http://mirror.ctan.org/systems/texlive/tlnet</code> 的别名.</p>
<p><strong>注意</strong> mirror.ctan.org 会被解析为多个不同的域名, 它们并不是完全同步的, 我们建议 (它们) 最多每天更新一次<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a>. 如果出现了问题, 我们建议您选择一个特定的镜像站. 全部的镜像站以及它们的状态会列在 <a href="https://ctan.org/mirrors/mirmon">镜像站列表</a>.</p>
<h4 id="大陆地区镜像">大陆地区镜像</h4>
<p>大陆用户可以将源指定为国内的镜像站, 在 CTAN 上可以找到 <a href="https://ctan.org/mirrors#Asia">亚洲镜像列表</a> . 下面列举出截止至 2021-03-04 可用的镜像站<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a>:</p>
<table>
<thead>
<tr class="header">
<th>镜像名</th>
<th>镜像地址</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><a href="https://developer.aliyun.com/mirror/">阿里云</a></td>
<td>https://mirrors.aliyun.com/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirror.bjtu.edu.cn/">北京交通大学</a></td>
<td>https://mirror.bjtu.edu.cn/ctan/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.bit.edu.cn/web/">北京理工大学</a></td>
<td>https://mirrors.bit.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.bfsu.edu.cn/">北京外国语大学</a></td>
<td>https://mirrors.bfsu.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.cqu.edu.cn/">重庆大学</a></td>
<td>https://mirrors.cqu.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.dgut.edu.cn/">东莞理工学院</a></td>
<td>https://mirrors.dgut.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.hit.edu.cn/">哈尔滨工业大学</a></td>
<td>https://mirrors.hit.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.huaweicloud.com/">华为云</a></td>
<td>https://mirrors.huaweicloud.com/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="http://mirrors.hust.edu.cn/">华中科技大学</a></td>
<td>http://mirrors.hust.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirror.lzu.edu.cn/">兰州大学</a></td>
<td>https://mirror.lzu.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.nju.edu.cn/">南京大学</a></td>
<td>https://mirrors.nju.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.tuna.tsinghua.edu.cn/">清华大学</a></td>
<td>https://mirrors.tuna.tsinghua.edu.cn/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.sjtug.sjtu.edu.cn/">上海交通大学</a></td>
<td>https://mirrors.sjtug.sjtu.edu.cn/ctan/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.geekpie.club/">上海科技大学</a></td>
<td>https://mirrors.geekpie.club/CTAN/systems/texlive/</td>
</tr>
<tr class="odd">
<td><a href="https://mirrors.cloud.tencent.com/">腾讯云</a></td>
<td>https://mirrors.cloud.tencent.com/CTAN/systems/texlive/</td>
</tr>
<tr class="even">
<td><a href="https://mirrors.ustc.edu.cn/">中国科学技术大学</a></td>
<td>https://mirrors.ustc.edu.cn/CTAN/systems/texlive/</td>
</tr>
</tbody>
</table>
<p>并用 <code>tlmgr option repository &lt;site&gt;/tlnet</code> 的方式进行设置.</p>
<h3 id="tlmgr-update---list"><code>tlmgr update --list</code></h3>
<p>仅列出所有可以被升级的内容.</p>
<h3 id="tlmgr-update---all"><code>tlmgr update --all</code></h3>
<p>将您本地的 TeX 安装内容与包管理库中的内容同步.</p>
<h3 id="tlmgr-info-what"><code>tlmgr info &lt;what&gt;</code></h3>
<p>列出 <code>&lt;what&gt;</code> 的详细信息, 比如它的安装状态以及介绍. 例如, 使用 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr info ctex</span><br></pre></td></tr></table></figure> 可以查看 <code>ctex</code> 宏集的详细信息: <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">package:     ctex</span><br><span class="line">category:    Package</span><br><span class="line">shortdesc:   LaTeX classes and packages <span class="keyword">for</span> Chinese typesetting</span><br><span class="line">longdesc:    ctex is a collection of macro packages and document classes <span class="keyword">for</span> LaTeX Chinese typesetting.</span><br><span class="line">installed:   Yes</span><br><span class="line">revision:    56705</span><br><span class="line">sizes:       src: 473k, doc: 1101k, run: 1749k</span><br><span class="line">relocatable: No</span><br><span class="line">cat-version: 2.5.5</span><br><span class="line">cat-license: lppl1.3c</span><br><span class="line">cat-topics:  chinese book-pub class</span><br><span class="line">cat-contact-bugs: https://github.com/CTeX-org/ctex-kit/issues</span><br><span class="line">cat-contact-home: http://www.ctex.org/HomePage</span><br><span class="line">cat-contact-repository: https://github.com/CTeX-org/ctex-kit</span><br><span class="line">collection:  collection-langchinese</span><br></pre></td></tr></table></figure> 对于 <code>tlmgr</code> 的更详细的用法, 请继续阅读.</p>
<h2 id="选项-options">选项 (OPTIONS)</h2>
<p>下列 <code>tlmgr</code> 的选项是<strong>全局</strong>选项, 不是特定于某一个命令的 (action specific). 对于所有的选项, 无论是全局的还是命令特定的, 都可以在 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr [option...] action [option...] [operand...]</span><br></pre></td></tr></table></figure> 的任意位置, 按任意顺序使用. 命令中第一个不是选项的变量将会成为这个命令的主要操作 (main action). 在所有的情况中 <code>-option</code> 与 <code>--option</code> 是等价的, <code>=</code> 可以放在一个选项名和它的值之间, 按 <code>key=value</code> 的格式使用.</p>
<h3 id="repository-urlpath"><code>--repository &lt;url|path&gt;</code></h3>
<p>指定一个宏包的仓库, 可以是本地文件夹 <code>&lt;path&gt;</code> 或者网络位置 <code>&lt;url&gt;</code>. 这个设置会临时覆盖掉 TeX Live Package Database (TLPDB, 由文件 <code>tlpkg/texlive.tlpdb</code> 给出) 中默认的宏包仓库.</p>
<p><strong>注意</strong> <code>--repository &lt;url|path&gt;</code> 命令只会修改当前运行的仓库选择, 如果想长期修改仓库, 可以使用 <code>option repository</code> (见 <a href="#action-option"><code>option</code></a>操作)</p>
<p>比如, 你可以这样选择一个特定的 CTAN 镜像</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-repository http://ctan.example.org/its/ctan/dir/systems/texlive/tlnet</span><br></pre></td></tr></table></figure>
<p>当然，我们必须指定一个真正的主机名及其特定的最高级的 CTAN 目录. 全部可用的 CTAN 镜像可以在 <a href="https://ctan.org/mirrors/mirmon">镜像站列表</a> 处查看.</p>
<p>下面是一个使用本地目录的例子:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-repository /<span class="built_in">local</span>/TL/repository</span><br></pre></td></tr></table></figure>
<p>剩下是一些特定的网络下的设置, 略过.</p>
<h3 id="gui-action"><code>--gui [action]</code></h3>
<p><code>tlmgr</code> 有两个 GUI 前端, <code>tlshell</code> 和 <code>tlcockpit</code>, 它们是由不同的程序启动的, 详情可以查看它们的文档.</p>
<p>与命令行界面一样, <code>tlmgr</code> 自己有一个图形界面. 我们可以用一个选项 <code>-gui</code> 来调用它. 如果后面还跟着一个操作 (action), 那么会直接在图形界面执行这个操作, 比如</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tlmgr --gui update</span><br></pre></td></tr></table></figure>
<p>会直接启动升级图形界面. 如果 <code>-gui</code> 后面没有跟着操作, 那么就直接打开 GUI 的主界面. 由于我不使用 GUI 界面, 这里就不对这一部分进行介绍, 详情可以参考 <a href="https://www.tug.org/texlive/doc/tlmgr.html#GUI-FOR-TLMGR">GUI FOR TLMGR</a></p>
<h3 id="gui-lang-llcode"><code>--gui-lang &lt;llcode&gt;</code></h3>
<p>通常情况来说, GUI 会从计算机中获取你使用的语言. 如果它没成功, 或者你想指定一个语言的 GUI 界面, 可以设置:</p>
<table>
<thead>
<tr class="header">
<th>语言</th>
<th>语言代码</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>英语</td>
<td><code>en</code>, 默认</td>
</tr>
<tr class="even">
<td>捷克语</td>
<td><code>cs</code></td>
</tr>
<tr class="odd">
<td>德语</td>
<td><code>de</code></td>
</tr>
<tr class="even">
<td>法语</td>
<td><code>fr</code></td>
</tr>
<tr class="odd">
<td>意大利语</td>
<td><code>it</code></td>
</tr>
<tr class="even">
<td>日语</td>
<td><code>ja</code></td>
</tr>
<tr class="odd">
<td>荷兰语</td>
<td><code>nl</code></td>
</tr>
<tr class="even">
<td>波兰语</td>
<td><code>pl</code></td>
</tr>
<tr class="odd">
<td>巴西葡萄牙语</td>
<td><code>pt_BR</code></td>
</tr>
<tr class="even">
<td>俄语</td>
<td><code>ru</code></td>
</tr>
<tr class="odd">
<td>斯洛伐克语</td>
<td><code>sl</code></td>
</tr>
<tr class="even">
<td>塞尔维亚语</td>
<td><code>sr</code></td>
</tr>
<tr class="odd">
<td>乌克兰语</td>
<td><code>uk</code></td>
</tr>
<tr class="even">
<td>越南语</td>
<td><code>vi</code></td>
</tr>
<tr class="odd">
<td>简体中文</td>
<td><code>zh_CN</code></td>
</tr>
<tr class="even">
<td>繁体中文</td>
<td><code>zh_TW</code></td>
</tr>
</tbody>
</table>
<h3 id="command-logfile-file"><code>--command-logfile &lt;file&gt;</code></h3>
<p>将 <code>tlmgr</code> 调用的所有程序 (<code>mktexlr</code>, <code>mtxrun</code>, <code>fmtutil</code>, <code>updmap</code>) 的输出写入一个文件 <code>&lt;file&gt;</code> 中, 默认位置为 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">TEXMFSYSVAR&#x2F;web2c&#x2F;tlmgr-commands.log</span><br></pre></td></tr></table></figure></p>
<h3 id="debug-translation"><code>--debug-translation</code></h3>
<p>一个协助翻译的选项, 略过</p>
<h3 id="machine-readable"><code>--machine-readable</code></h3>
<p>将标准输出写成一个更适合计算机解析的格式</p>
<h3 id="package-logfile-file"><code>--package-logfile &lt;file&gt;</code></h3>
<p><code>tlmgr</code> 将所有宏包操作 (<code>install</code>, <code>remove</code>, <code>update</code>, <code>failed updates</code>, <code>failed restores</code>) 写入一个单独的日志文件, 默认位置为 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">TEXMFSYSVAR&#x2F;web2c&#x2F;tlmgr.log</span><br></pre></td></tr></table></figure> 这个选项让你可以为日志文件指定另一个路径.</p>
<h3 id="pause"><code>--pause</code></h3>
<p>让 <code>tlmgr</code> 在退出之前等待用户输入, 在 Windows 上可以避免命令行的消失.</p>
<h3 id="usermode"><code>--usermode</code></h3>
<p>在这次运行 <code>tlmgr</code> 时启用 user mode. 见 <a href="#user-mode"><code>USER MODE</code></a></p>
<h3 id="usertree-dir"><code>--usertree &lt;dir&gt;</code></h3>
<p>在 <code>&lt;dir&gt;</code> 中创建 user mode 的树. 见 <a href="#user-mode"><code>USER MODE</code></a></p>
<h3 id="verify-repononemainall"><code>--verify-repo=&lt;none|main|all&gt;</code></h3>
<p>没看懂</p>
<p>TeX Live 程序的标准选项也可以使用, 比如 <code>--help/-h/-?</code>, <code>--version</code>, <code>-q</code> (无提示信息), <code>-v</code> (显示调试信息)</p>
<h2 id="操作-actions">操作 (ACTIONS)</h2>
<h3 id="help"><code>help</code></h3>
<p>显示一个帮助信息并退出, 与 <code>--help</code> 选项的作用相同, 实际就是打开一个与 <a href="https://www.tug.org/texlive/doc/tlmgr.html"><code>tlmgr</code>帮助文档</a> 相同的帮助信息.</p>
<h3 id="version"><code>version</code></h3>
<p>给出版本信息, 与 <code>--version</code> 选项相同, 如果也使用了 <code>-v</code> 选项, 那么所使用模块的版本信息也会被打印.</p>
<h3 id="backup"><code>backup</code></h3>
<h4 id="backup-option..-pkg-all"><code>backup [option..] &lt;pkg|-all&gt;</code></h4>
<p>如果没有指定 <code>--clean</code> 选项, 那么这个操作会创建一个宏包 <code>&lt;pkg&gt;</code> 的备份, 如果使用了 <code>-all</code> 选项, 则创建一个全部宏包的备份.</p>
<p>备份文件的位置将由 <code>--backupdir &lt;dir&gt;</code> 指定, 前提是 <code>&lt;dir&gt;</code> 存在并且可写. 如果没有指定 <code>--backupdir</code>, 那么就使用 TLPDB 中设置的 <code>backupdir</code>, 如果二者皆空, 那么将不会创建备份.</p>
<p>安装程序将 <code>backupdir</code> 默认设置在了 TeX Live 根目录下的 <code>./tlpkg/backups</code> 中, 所以如果没有额外的设置, <code>backupdir</code> 总是存在. 查看 <a href="#action-option"><code>option</code></a> 以获得更多信息.</p>
<p>如果指定了 <code>--clean</code> 选项, 那么备份文件将被删除, 而不是被保存. 一个可选的整数值 <code>N</code> 可以指定在清理时保留的备份数. 如果没有指定 <code>N</code>, 那么将使用 <code>autobackup</code> 的值, <code>autobackup</code> 默认在 TLPDB 中为 <code>1</code>. 如果二者皆空, 那么将会报错. 查看 <a href="#action-option"><code>option</code></a> 来获得更多关于删除备份文件的详细信息.</p>
<p><code>backup</code> 操作的特定选项:</p>
<h4 id="backupdir-dir"><code>--backupdir &lt;dir&gt;</code></h4>
<p>覆盖 TLPDB 中 <code>backupdir</code> 的值. 变量 <code>&lt;dir&gt;</code> 必须要指定, 这是备份文件存放的路径, 它必须要存在且可写</p>
<h4 id="all"><code>--all</code></h4>
<p>如果没有指定 <code>--clean</code> 选项, 那么创建一个 TeX Live 安装过的所有宏包的备份, 这会消耗大量的存储空间与时间. 如果指定 <code>--clean</code> 选项, 所有的备份将被删除.</p>
<h4 id="cleann"><code>--clean[=N]</code></h4>
<p>删除备份目录 <code>backupdir</code> 中的旧备份, 而不是创建备份. 可选参数整数值 <code>N</code> 会覆盖 TLPDB 中 <code>autobackup</code> 的值. 如果使用这个选项, 那么必须要指定 <code>--all</code> 或者一列宏包.</p>
<h4 id="dry-run"><code>--dry-run</code></h4>
<p>备份或删除的操作将被打印在终端而不真正进行备份或者删除, 如 <code>tlmgr --dry-run backup ctex</code> 会打印出 <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">❯❯ tlmgr --dry-run backup ctex</span><br><span class="line">would save current status of ctex to d:/texlive/2020/tlpkg/backups/ctex.r56705.tar.lz4</span><br><span class="line">no action taken due to --dry-run</span><br></pre></td></tr></table></figure></p>
<p><span id="action-option"> <code>option</code> </span> <span id="user-mode"> <code>USER MODE</code> </span></p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>mirror.ctan.org resolves to many different hosts, and they are not perfectly synchronized; we recommend updating only daily (at most), and not more often.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>这个表格来自 <a href="https://github.com/OsbertWang/install-latex-guide-zh-cn">install-latex-guide-zh-cn</a><a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>LaTeX</category>
        <category>帮助文档</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
        <tag>帮助文档</tag>
        <tag>tlmgr</tag>
      </tags>
  </entry>
  <entry>
    <title>欢迎来到我的 GitHub.io</title>
    <url>/2020-12-12-welcome/</url>
    <content><![CDATA[<h1 id="这里是无锤乙醇的-github.io">这里是无锤乙醇的 <code>GitHub.io</code></h1>
<p>花了小一天来折腾 Hexo NexT 主题, 也算是把基本配置弄好了, 咕了好久的博客也是搭好了, 还有一些坑没有填, 比如<a href="https://blog.dlzhang.com/posts/32/">自定义侧边栏图标</a>, 有时间慢慢折腾吧.</p>
<p>接下来会把简书里的东西搬到这里, <del><a href="https://syvshc.gitee.io">Gitee Pages</a> 是主要更新的地方, 这边会同步更新.</del> 就搁这更新了, Gitee 不折腾了</p>
<p>并且这篇文章会被用做测试</p>
<span id="more"></span>
<h1 id="测试">测试</h1>
<p>更新了字体, <a href="https://leay.net/2020/02/14/hexo-next-font/">字体配置方法</a></p>
<p>this is a footnote test <a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<p>this is a equation <span class="math inline">\(ax^{2}+bx+c=0\,(a\ne 0)\)</span>, which solution is <span class="math display">\[x_{1, 2}=\frac{-b\pm\sqrt{b^{2}-4ac}}{2a}\]</span></p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>footnote<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>杂项</category>
      </categories>
  </entry>
  <entry>
    <title>latexmk 的学习</title>
    <url>/2021-03-04-learn-latexmk/</url>
    <content><![CDATA[<p>其实是把翻译给咕掉了, 先把实用的东西写了, 方便查阅, 剩下的嘛, 考完研再说吧, 也没准哪天心血来潮就写了</p>
<p>先把这个东西写了, 随用随查, 然后也说一下遇到的坑什么的(如果有的话)</p>
<span id="more"></span>
<h2 id="命令行中的-latexmk">命令行中的 <code>latexmk</code></h2>
<p><code>latexmk</code> 的通用格式为 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latexmk [options] [file]</span><br></pre></td></tr></table></figure></p>
<p>其中 <code>file</code> 可以直接写文件名, 如果直接写 <code>foo</code> 的话, <code>latexmk</code> 会按照 <code>foo.tex</code> 来处理. 有一些符号在文件名中是不能用的: <code>$</code>, <code>%</code>, <code>\</code>,`(这个东西不能放在代码块里……), <code>&amp;</code>这个符号不能作为文件名的开头, 还有一些控制字符也不可以, 不过我估计没人会闲的用控制字符来做 <code>.tex</code> 的文件名...</p>
<h3 id="几个常用的-option">几个常用的 <code>option</code></h3>
<h4 id="auxdirfoo-或者--aux-directoryfoo"><code>-auxdir=FOO</code> 或者 <code>-aux-directory=FOO</code></h4>
<p>设置存放辅助文件的文件夹, 但是这个命令只能在 <code>MiKTeX</code> 版本的 <code>latex</code>中使用, 我这里是 <code>TeX Live</code>, 没有办法做测试.</p>
<h4 id="bibtex"><code>-bibtex</code></h4>
<p>当源文件需要用 <code>.bbl</code> 文件作为参考文献的时候, 运行 <code>bibtex</code> 或者 <code>biber</code> 来更新 <code>.bbl</code> 文件. 这个选项可以将 <code>latexmkrc</code> 中的 <code>$bibtex_use</code> 的值设置为 <code>2</code> 来实现.</p>
<h4 id="bibtex-"><code>-bibtex-</code></h4>
<p>从不运行 <code>bibtex</code> 或者 <code>biber</code>. 同时将 <code>.bbl</code> 文件看做珍贵的 ( 原文为 precious ), 也就是在执行清理命令的时候不会删除 <code>.bbl</code> 文件. 如果我们接受到的文件中只有 <code>.bbl</code> 而没有 <code>.bib</code> 的时候会启用这个选项. 这个选项可以将<code>latexmkrc</code> 中的 <code>$bibtex_use</code> 的值设置为 <code>0</code> 来实现.</p>
<h4 id="bibtex-cond"><code>bibtex-cond</code></h4>
<p>如果源文件中存在 <code>.bib</code> 文件, 则运行 <code>bibtex</code> 或 <code>biber</code> 来重写 <code>.bbl</code> 文件, 如果没有 <code>.bib</code> 文件, 则不运行 <code>bibtex</code> 或 <code>biber</code>, 这个选项也会将 <code>.bbl</code> 文件看做珍贵的. 这个选项可以将<code>latexmkrc</code> 中的 <code>$bibtex_use</code> 的值设置为 <code>1</code> 来实现.</p>
<h4 id="command"><code>-command</code></h4>
<p>列出 <code>latexmk</code> 在处理文件时可以使用的命令, 然后退出.</p>
<h4 id="c"><code>-c</code></h4>
<p>清理所有由 <code>latex</code> , <code>bibtex</code>, 以及 <code>biber</code> 生成的可再生的文件, 除了 <code>.dvi</code>, <code>postscript</code>, 和 <code>.pdf</code> 文件. 会删除的文件有 <code>.log</code>, <code>.aux</code>, <code>latexmk</code> 创建的数据文件, 还有 <code>@generated_exts</code> 中指定扩展名的文件, 同时还有 <code>$clean_ext</code> 指定的文件.</p>
<h4 id="c-1"><code>-C</code></h4>
<p>清理 <code>-c</code> 选项中的所有文件, 以及 <code>.pdf</code>, <code>.dvi</code>, <code>postscript</code> 文件, 以及 <code>$clean_full_ext</code> 指定的文件.</p>
<h4 id="cd"><code>-cd</code></h4>
<p>在进行处理之前切换到有源程序的文件夹, 并将所有的文件生成在这个文件夹里. 举例: 如果你的文件夹结构为 <code>parent/subfolder/main.tex</code>, 你在 <code>parent</code> 文件夹下, 命令行执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latexmk -cd -xelatex subfolder&#x2F;main</span><br></pre></td></tr></table></figure>
<p>后, 会进入 <code>subfolder</code> 文件夹进行编译, 并将辅助文件和输出文件生成在 <code>subfolder</code> 文件夹下. 这里注意路径的分隔符要使用 <code>/</code> , 哪怕是 <code>Windows</code>.</p>
<h4 id="f"><code>-f</code></h4>
<p>强制执行 <code>latexmk</code> 哪怕遇到了 <code>error</code>. 一般情况下, 当 <code>latexmk</code> 遇到了 <code>latex</code> 或者其他程序在<strong>接下来的运行中</strong>无法处理的问题的时候, 将不会给出处理结果.</p>
<p><strong>接下来的运行</strong>指的是 如果没有 <code>error</code> 出现, 运行其他程序或者重新运行<code>latex</code> 的时候会编译完成 ( 这段其实我没看懂, 原文是 "Further processing" means the running of other Programs or the rerunning of latex (etc) that would be done if no errors had occurred. ) 额外地, 如果你想让 <code>latex</code> 程序遇到 <code>error</code> 之后不出现给用户处理 <code>error</code> 的暂停 , 你需要使用一些可以传递给程序的选项, 比如 <code>-interaction=nonstopmode</code>. 举例: 当文档里有一个严重的错误, 比如没有定义的命令, 使用 <code>-f</code> 的话</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latexmk -f -xelatex main</span><br></pre></td></tr></table></figure>
<p>在遇到错误的时候依旧会暂停来等待用户处理, 而使用 <code>-interaction=nonstopmode</code> 的话</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latexmk -interction&#x3D;nonstopmode -xelatex main</span><br></pre></td></tr></table></figure>
<p><code>latexmk</code> 会一气呵成运行到最后, 哪怕没有文件输出.</p>
<p>#### <code>-g</code> 强制完整地运行一遍 <code>latexmk</code>, 哪怕在 <code>latexmk</code> 觉得源文件自从上次编译之后没有改动</p>
<h4 id="h--help"><code>-h</code>, <code>-help</code></h4>
<p>打印 <code>latexmk</code> 的帮助信息.</p>
<h4 id="jobnamestring"><code>-jobname=STRING</code></h4>
<p>把输出文件中的文件名设置为 <code>STRING</code>, 而不是源文件名, 这个 <code>STRING</code> 中不能带空格. 如果要使用占位符 <code>%A</code> 的话也可以, 比如</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latexmk -xelatex -jobname&#x3D;%A-xelatex main</span><br></pre></td></tr></table></figure>
<p>这样输出文件名就变成了 <code>main-xelatex.***</code>.</p>
<h4 id="outdirfoo-或者--output-directoryfoo"><code>-outdir=FOO</code> 或者 <code>-output-directory=FOO</code></h4>
<p>设置存放所有输出文件的文件夹.</p>
<h4 id="pdf"><code>-pdf</code></h4>
<p>当使用 <code>pdflatex</code> 的时候输出 <code>.pdf</code> 文件.</p>
<h4 id="pdflatex"><code>-pdflatex</code></h4>
<p>使用 <code>pdflatex</code> 编译, 并且关闭 <code>.dvi</code> 和 <code>.ps</code> 文件的生成.</p>
<h4 id="pv"><code>-pv</code></h4>
<p>运行预览程序, 因为编译中文文档用的 <code>xelatex</code> 不会生成 <code>.dvi</code> 和 <code>.ps</code> 文件, 所以也不存在预览哪个文件的问题, 直接就是运行 <code>.pdf</code> 文件的预览.</p>
<h4 id="r-rcfile"><code>-r &lt;rcfile&gt;</code></h4>
<p>按 <code>&lt;rcfile&gt;</code> 中的内容执行 <code>latexmk</code>. 但是要注意调用顺序, <code>latexmk</code> 总是会先调用标准初始的 <code>latexmkrc</code> 文件, 如果不存在初始的 <code>latexmkrc</code> 文件的情况下再去调用 <code>&lt;rcfile&gt;</code>. 但是如果 <code>&lt;rcfile&gt;</code> 中指定了一个初始的 <code>latexmkrc</code> 文件, 那么这个文件会在所有的标准初始的 <code>latexmkrc</code> 文件之前运行.</p>
<h4 id="v--version"><code>-v</code>, <code>-version</code></h4>
<p>打印 <code>latexmk</code> 的版本号.</p>
<h4 id="xelatex"><code>-xelatex</code></h4>
<p>使用 <code>xelatex</code> 编译程序, 同时关闭 <code>.dvi</code> 和 <code>.ps</code> 的生成.</p>
<p>以上是我在<strong>阅读文档</strong>的过程中觉得我会用的到的选项, 如果有更多的话会再补充</p>
<h2 id="rc-文件中的-latexmk"><code>RC</code> 文件中的 <code>latexmk</code></h2>
<h3 id="如何在-rc-文件中设置变量">如何在 <code>RC</code> 文件中设置变量</h3>
<p>变量的格式应如下</p>
<p>设置一个值为<strong>字符串</strong>的变量:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$bibtex&#x3D;&#39;bibtex %O %B&#39;;</span><br></pre></td></tr></table></figure>
<p>设置一个值为<strong>数字</strong>的变量:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$preview_mode&#x3D;1;</span><br></pre></td></tr></table></figure>
<p>设置一个值为<strong>数组</strong>的变量:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@default_files&#x3D;(&#39;paper&#39;, &#39;paper1&#39;);</span><br></pre></td></tr></table></figure>
<p>也可以用下面的语句来给数组变量添加值:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">push @default_files, &#39;paper2&#39;;</span><br></pre></td></tr></table></figure>
<p>可以注意到简单的<strong>标量</strong>变量的名字的开头都是 <code>$</code>, 而数组变量的名字的开头是 <code>@</code>, 每个语句都以分号 <code>;</code> 结尾. 字符串应该用<strong>单引号</strong>括起来, 不建议使用双引号.</p>
<h3 id="命令规定的格式">命令规定的格式</h3>
<p>一些变量会设置 <code>latexmk</code> 用于工作的命令, 比如通过 <code>.tex</code> 文件生成 <code>.dvi</code> 文件, 或者去浏览一个 <code>.ps</code> 文件.</p>
<h4 id="占位符">占位符</h4>
<p>如果你想让 <code>latexmk</code> 在通常是 <code>latex</code> 的位置使用 <code>elatex</code> 命令, 而且想让它使用 <code>--shell-escape</code> 选项, 要像下面这么写</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">latex&#x3D;&#39;elatex --shell-escape %O %S&#39;;</span><br></pre></td></tr></table></figure>
<p>那两个前面带 <code>%</code> 的就是<strong>占位符</strong>, 在运行命令之前他们会被替换为适当的值. 因此 <code>%S</code> 会被替换为 <code>elatex</code> 应用的源文件名, <code>%O</code> 会被替换为任何 <code>latexmk</code> 将作用在这个命令上的选项, 比如如果你在调用 <code>latexmk</code> 的时候使用了 <code>-silent</code> 选项, <code>%O</code> 会被替换为 <code>-interaction=batchmode</code>. 下面列出可用的占位符 + <code>%A</code>: 主 <code>.tex</code> 文件的文件名. 不同于 <code>%R</code>, <code>%A</code> 不会被设置别名(jobname) 而受影响; + <code>%B</code>: 最近命令中的基础名 (base name). 比如一个 <code>document.ps</code> 文件从一个 <code>document.dvi</code> 转化得到, 那么这个文件名就是 <code>document</code> + <code>%D</code>: 终点 (destination) 文件名. 比如一个 <code>.ps</code> 文件由一个 <code>.dvi</code> 文件转化得到, 那么终点文件名就是这个 <code>.ps</code> 文件名. + <code>%O</code>: 例子中提到的选项. + <code>%P</code>: 如果变量 <code>$pre_tex_code</code> 不空, 那么 <code>%P</code> 会被替换为 <code>$pre_tex_code</code> 中 <code>\input&#123;SOURCE&#125;</code> 后面的内容, 其中 <code>SOURCE</code> 是资源文件 (source file) 的名字. 这让 TeX 代码可以在资源文件被读入之前就传给 <code>*latex</code> 引擎. 如果 <code>$pre_tex_code</code> 为空, 那么 <code>%P</code> 和 <code>%S</code>等价. + <code>%R</code>: 根文件名, 这是主 <code>.tex</code> 文件的基础名, 但是这个值可以被 <code>-jobname</code> 选项或者 <code>$jobname</code> 变量改变. + <code>%S</code>: 源文件名. 比如当转化一个 <code>.dvi</code> 文件到 <code>.ps</code> 的时候, <code>.dvi</code> 的文件名就是源文件名. + <code>%T</code>: The name of the primary <code>.tex</code> file. + <code>%U</code>: 如果变量 <code>$pre_tex_code</code> 不空, 那么这个值就被传递给 <code>%U</code>, 如果为空, 那么它被替换为一个空字符 (null string). + <code>%Y</code>: 辅助文件所在的文件夹名. 如果文件夹名不是以合法符号结束, 那么就会被添加一个文件夹分隔符 <code>/</code>. 注意: 如果设置了 <code>$out_dir</code> 却没设置 <code>$aux_dir</code>, 那么<code>latexmk</code> 就把 <code>$aux_dir</code> 设置为 <code>$out_dir</code>. + <code>%Z</code>: 输出文件夹名. 如果 <code>$out_dir</code> 不空, 且没以合法符号结束, 那么就会被添加一个文件夹分隔符 <code>/</code>.</p>
<p>(说实在的我暂时没分清这么多 name 到底是什么 name, 比如 basename, roof filename, primary tex file...)</p>
<p>如果有什么原因你要使用一个 <code>%</code> 字符, 还不是上面标出的情况, 那么用 <code>%%</code>.</p>
<p><strong>注意</strong>: 在文件名传递的过程中会被适当地添加引号, 所以不要自行添加引号, 即使文件名中有空格. ( 如果你的 TeX 文件名中有空格, 那么一些过老版本的 TeX 程序可能不会很好地处理它们. ) 如果 <code>latexmk</code> 的引号不能正确地工作, 你也可以将它关闭, 在变量 <code>$quote_filenames</code> 的设置中.</p>
<h2 id="更新">20200406 更新</h2>
<p>昨天在使用 <code>latexmk -xelatex -pvc main</code> 的时候发现 <code>SumatraPDF</code> 不能主动更新 <code>PDF</code> 文件, 而使用 <code>latexmk -pdflatex -pvc main</code> 就可以更新, 甚至将 <code>rc</code> 文件中的 <code>$pdflatex</code> 设置为</p>
<figure class="highlight latex"><table><tr><td class="code"><pre><span class="line"><span class="built_in">$</span>pdflatex=&quot;xelatex -file-line-error -halt-on-error  -interaction=nonstopmode -synctex=1 <span class="comment">%O %S&quot;;</span></span><br></pre></td></tr></table></figure>
<p>都可以更新 <code>pdf</code> 文件. 原因是 <code>latexmk</code> 选项中的 <code>-xelatex</code> 的默认选项是 <code>xelatex %O %S</code> 下面直接引用 <code>texdoc latexmk</code> 中的原文</p>
<blockquote>
<p><strong>Note about <code>xelatex</code></strong>: <code>latexmk</code> uses <code>xelatex</code> to make an <code>.xdv</code> rather than <code>.pdf</code> file, with the <code>.pdf</code> file being created in a separate step. This is enforced by the use of the <code>-no-pdf</code> option. If <code>%O</code> is part of the command for invoking <code>xelatex</code>, then latexmk will insert the <code>-no-pdf</code> option automatically, otherwise you must provide the option yourself.</p>
</blockquote>
<p>而 <code>-no-pdf</code> 是 <code>xelatex</code> 特有的选项.</p>
<p>综上所述, 产生这个问题的原因是: <strong>在使用 <code>latexmk</code> 的 <code>-xelatex</code> 选项时, <code>latexmk</code> 会执行 <code>xelatex %O %S</code>, 而这个 <code>%O</code> 中带了 (<code>latexmk</code> 选项) 中的 <code>-no-pdf</code> 选项, 于是在没有配置 <code>rc</code> 文件的时候, <code>latexmk -xelatex main</code> = <code>xelatex -no-pdf -record main</code></strong>.</p>
<p>问题出现了就可以给出解决办法:</p>
<ol type="1">
<li>将 <code>rc</code> 中的 <code>$xelatex</code> 选项的配置为不带 <code>%O</code> 的命令, 如 <code>$xelatex="xelatex %S"</code> 即可, 但是这么做的后果是不能将临时添加在 <code>latexmk</code> 中的 <code>xelatex</code> 的选项传递给 <code>xelatex</code>, 比如像上面一样配置 <code>$xelatex</code>, 那么在使用 <code>latexmk -synctex=1 main</code> 的时候, 不会执行 <code>-synctex=1</code> 这个选项.</li>
<li>修改 <code>rc</code> 文件中的 <code>$pdflatex</code> 选项为 <code>$pdflatex="xelatex %O %S"</code> 这样可以接受临时添加的选项, 后果是不能使用 <code>pdflatex</code> 编译.</li>
</ol>
]]></content>
      <categories>
        <category>LaTeX</category>
        <category>帮助文档</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
        <tag>帮助文档</tag>
        <tag>latexmk</tag>
      </tags>
  </entry>
</search>
